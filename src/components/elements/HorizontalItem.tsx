import Text from "./Text";
interface HorizontalItemProps {
  // imgUrl? : string;
  // title : string;
  // className? : string;
  // size : number
}

const HorizontalItem = ({ } : HorizontalItemProps) => {
  return (
    <div>
      HorizontalItem
    </div>
  );
};

export default HorizontalItem;